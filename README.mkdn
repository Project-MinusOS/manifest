MinusOS
===========
To get started
--------------
```
repo init -u https://github.com/Project-MinusOS/manifest.git -b cirno --git-lfs
```
Sync
--------------
```
repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags
```
