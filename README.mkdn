MinusOS
===========
To get started
--------------
```
repo init -u https://github.com/Project-MinusOS/manifest.git -b balsam_v2 --git-lfs
```
Sync
--------------
```
repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags
```
